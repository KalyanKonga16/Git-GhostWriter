services:
  - type: worker
    name: git-ghostwriter-worker
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: celery -A worker worker --loglevel=info --pool=solo
    envVars:
      - fromGroup: redis
      - fromGroup: groq
      - fromGroup: r2
